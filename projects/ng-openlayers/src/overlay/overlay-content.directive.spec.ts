import { OverlayContentDirective } from './overlay-content.directive';

describe('OverlayContentDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayContentDirective();
    expect(directive).toBeTruthy();
  });
});
