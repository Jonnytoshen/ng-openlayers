import { CoordinateToStringHdmsPipe } from './coordinate-to-string-hdms.pipe';

describe('CoordinateToStringHdmsPipe', () => {
  it('create an instance', () => {
    const pipe = new CoordinateToStringHdmsPipe();
    expect(pipe).toBeTruthy();
  });
});
