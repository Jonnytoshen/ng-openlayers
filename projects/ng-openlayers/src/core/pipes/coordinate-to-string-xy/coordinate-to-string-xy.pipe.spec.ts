import { CoordinateToStringXYPipe } from './coordinate-to-string-xy.pipe';

describe('CoordinateToStringXYPipe', () => {
  it('create an instance', () => {
    const pipe = new CoordinateToStringXYPipe();
    expect(pipe).toBeTruthy();
  });
});
