import { InheritExtentPipe } from './inherit-extent.pipe';

describe('InheritExtentPipe', () => {
  it('create an instance', () => {
    const pipe = new InheritExtentPipe();
    expect(pipe).toBeTruthy();
  });
});
